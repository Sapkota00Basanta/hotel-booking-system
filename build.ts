// Build Scripts using ShellJS Package
// const shell = require('shelljs');
// const config = './tsconfig/json';
// const outDir = config.compilerOptions.outDir;

// shell.rm('-rf', outDir);
// shell.mkdir(outDir);
// shell.mkdir('-p', `${outDir}/commom/swagger`);


// s.rm('-rf', outDir);
// s.mkdir(outDir);
// s.mkdir('-p', `${outDir}/common/swagger`);
// s.cp('server/common/api.yml', `${outDir}/common/api.yml`);
// s.cp("-R", "server/toc_template", `${outDir}`);
// s.cp('package.json', `${outDir}`);
// s.cp('yarn.lock', `${outDir}`);
// s.mkdir('-p', `${outDir}/public/pdf`)
// s.cp('server/.env', `${outDir}/.env`);
// s.cp('server/common/details.json.enc', `${outDir}/common/details.json.enc`)